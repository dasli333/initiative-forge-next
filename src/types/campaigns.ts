import type { Campaign } from '@/types';

/**
 * Campaign ViewModel for UI display
 * Currently identical to Campaign type, but separated for future extensions
 */
export type CampaignViewModel = Campaign;
